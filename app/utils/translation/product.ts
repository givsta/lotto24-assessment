import { createTranslation } from '@/app/utils/translation/i18n';

export const headline1 = createTranslation({ de: 'Have a look at our {prefix} products!' });
export const headline2 = createTranslation({ de: 'People also bought' });
